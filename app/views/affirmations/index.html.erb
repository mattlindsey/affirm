<div class="max-w-4xl mx-auto p-6">
  <h1 class="text-4xl font-bold text-center text-gray-800 mb-8">Daily Affirmations</h1>
  
  <% if @affirmations.any? %>
    <div class="space-y-6">
      <% @affirmations.each do |affirmation| %>
        <div class="bg-gray-50 border-l-4 border-blue-500 p-6 rounded-r-lg shadow-md hover:shadow-lg transition-shadow duration-200">
          <p class="text-lg text-gray-800 leading-relaxed mb-3"><%= affirmation.content %></p>
          <small class="text-sm text-gray-500">Created: <%= affirmation.created_at.strftime("%B %d, %Y") %></small>
        </div>
      <% end %>
    </div>
  <% else %>
    <p class="text-center text-gray-500 italic text-lg mt-10">No affirmations yet. Add some positive thoughts to get started!</p>
  <% end %>
</div>
