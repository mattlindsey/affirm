<div class="max-w-4xl mx-auto p-6">
  <div class="text-center mb-8">
    <h1 class="text-4xl font-bold text-gray-800 mb-4">Gratitude</h1>
    <div class="mb-6 space-x-4">
      <%= link_to "📝 Create todays gratitudes", create_gratitude_path, 
          class: "inline-block bg-green-600 hover:bg-green-700 text-white font-semibold py-3 px-6 rounded-lg transition-colors duration-200" %>
      <%= link_to "🎲 Get Random Gratitude", gratitude_random_path, 
          class: "inline-block bg-blue-600 hover:bg-blue-700 text-white font-semibold py-3 px-6 rounded-lg transition-colors duration-200" %>
    </div>
  </div>

  <% if @gratitudes.any? %>
    <div class="space-y-6">
      <% @gratitudes.each do |gratitude| %>
        <div class="bg-gray-50 border-l-4 border-green-500 p-6 rounded-r-lg shadow-md hover:shadow-lg transition-shadow duration-200">
          <p class="text-lg text-gray-800 leading-relaxed mb-3"><%= gratitude.content %></p>
          <small class="text-sm text-gray-500">Created: <%= gratitude.created_at.strftime("%B %d, %Y") %></small>
        </div>
      <% end %>
    </div>
  <% else %>
    <p class="text-center text-gray-500 italic text-lg mt-10">No gratitudes yet. Create today's gratitudes to get started!</p>
  <% end %>
</div>


